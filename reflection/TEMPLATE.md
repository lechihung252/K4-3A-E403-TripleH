# Reflection cá nhân — [Họ và tên] · [Mã học viên]

> Copy file này thành `<ten-cua-ban>.md`, điền theo 4 mục dưới. Viết thật, viết ngắn (1–2 trang), trỏ vào file/commit cụ thể trong repo.
> Rubric: `04-rubric.md` → "Reflection cá nhân": vai trò + phần mình làm + AI hỗ trợ thế nào + một bài học từ case fail của chính nhóm.
> Lúc pitch (CP6) giám khảo có thể hỏi bất kỳ phần nào có tên bạn — không giải thích được thì 0 điểm phần đó.

## 1. Vai trò trong nhóm

- Vai trò chính (theo bảng phân công ở `README.md`):
- Vì sao nhận vai trò này / nó liên quan gì đến thế mạnh của mình:

## 2. Phần mình đã làm

Liệt kê từng việc, mỗi việc trỏ vào bằng chứng trong repo (file, thư mục, commit, số liệu).

| Việc | Bằng chứng trong repo | Kết quả / con số |
|---|---|---|
| Ví dụ: gán nhãn tay 63 menu của bot cũ | `docs/nhan-tay-63-menu.md`, `docs/menu-labels.csv` | 20% câu trả lời là hỏi lại thừa |
| ... | ... | ... |

Một quyết định mình đã tự đưa ra (và vì sao):

## 3. AI đã hỗ trợ mình thế nào

- Tool đã dùng (Claude Code / Cursor / ChatGPT / Gemini / v0 ...):
- Dùng ở bước nào, cho việc gì:
- Chỗ AI làm tốt:
- Chỗ AI làm sai hoặc mình phải tự kiểm / tự sửa (kể ít nhất 1 ví dụ cụ thể):
- Cách mình kiểm tra output của AI trước khi đưa vào repo:

## 4. Một bài học từ case fail của chính nhóm

Chọn **một** case fail thật của nhóm (xem `spec.md` §7 Changelog, `eval/run-*.md`, `eval/trace/`), kể theo cấu trúc:

- **Chuyện gì xảy ra:** (ví dụ: `run-openai-1` 5/5 case CLARIFY sai vì model không bao giờ chọn nhãn này; hoặc lượt 100% hoá ra do regex `policyGuardrail` viết theo golden set — vi phạm "không hardcode"; hoặc M61735 "không đăng nhập được Phoenix" bị gán `personal_data` thay vì hỏi lại)
- **Nhóm phát hiện bằng cách nào:**
- **Đã sửa ra sao:**
- **Bài học rút ra cho lần sau (1–2 câu):**

## 5. Nếu làm lại / có thêm 1 tuần *(tuỳ chọn)*

- Một việc mình sẽ làm khác đi:
